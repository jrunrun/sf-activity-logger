version: "3.3"
   
services:
  python-env:
    container_name: python-script
    build:
      context: .
      dockerfile: Dockerfile
    command: python bulkAPI_project_work_logger.py
    volumes:
      - .:/code
